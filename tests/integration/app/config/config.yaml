framework:
    secret: 'test'
    test: true

monolog:
    handlers:
        main:
            type: stream
            path: "%kernel.logs_dir%/%kernel.environment%.log"
            level: debug

f1_monkey_eve_esi:
    oauth:
        callback_url: 'callback'
        client_id: 'client_id'
        client_secret: 'client_secret'

services:
    _defaults:
        public: true

    test.f1monkey.eve_esi.guzzle:
        alias: f1monkey.eve_esi.guzzle

    test.f1monkey.eve_esi.oauth_service:
        alias: F1Monkey\EveEsiBundle\Service\OAuth\OAuthServiceInterface